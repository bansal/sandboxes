<script setup lang="ts">
useHead({
  titleTemplate: (title) =>
    title ? `${title} — Sandboxes` : "Compare Sandbox Pricing",
  meta: [
    { charset: "utf-8" },
    { name: "viewport", content: "width=device-width, initial-scale=1" },
  ],
  link: [{ rel: "icon", href: "/favicon.ico" }],
  htmlAttrs: {
    lang: "en",
  },
});
</script>
<template>
  <UApp :toaster="{ position: 'top-center' }">
    <NuxtLoadingIndicator />
    <NuxtRouteAnnouncer />
    <NuxtLayout>
      <NuxtPage />
    </NuxtLayout>
  </UApp>
</template>
